name: parkplannerproject
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.2 <3.0.0'

dependencies:
  cloud_firestore: ^4.4.5
  firebase_auth: ^4.3.0
  firebase_core: ^2.8.0
  flutter:
    sdk: flutter
  geocoding: ^2.0.1
  geolocator: ^9.0.2
  latlng: ^1.0.0
  logger: ^1.3.0
  geocoder: ^0.2.1
  flutter_map: ^3.0.0
  provider: ^6.0.5
  flutter_geocoder: ^0.2.2-nullsafety
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true